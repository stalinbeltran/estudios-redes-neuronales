# Bitácora — septiembre de 2026

Las reglas, en [`../README.md`](../README.md). La única que importa al escribir:
**sólo se añaden filas; una fila escrita no se toca nunca más.** Si resultó falsa, se añade otra
que lo diga.

Horas en **UTC**. La zona del usuario es UTC-5, así que 12:00 UTC = 07:00 de la mañana.

| Cuándo | Repo | Qué se pidió / qué pasó | Qué se halló |
|---|---|---|---|
| 09-11 ~12:00 | `digital-ocean-dropplet-auto-launching` | **Petición:** «ayer en la tarde le pedí a Claude unos cambios, y luego de eso al destruir el dev y tratar de lanzarlo dejó de funcionar. Revisa, encuentra la falla, y haz que funcione.» | — |
| 09-11 ~12:05 | ídem | Se buscó el rastro del fallo | Estaba en el **journal del Lanzador del mini**, no en la laptop: dos `launch dev` (09-10 23:48 y 09-11 01:36 UTC) muertos a los ~900 s con una sola línea, «Se agotó la espera a que el droplet terminase de instalar las herramientas». Nada más: ni si la máquina iba lenta, ni si estaba atascada, ni si la sonda llegaba |
| 09-11 ~12:10 | ídem | Se comprobó si lo habían causado los cambios de la tarde anterior | **No.** `cloud-init.yaml` no se tocaba desde el 12-ago, y el diff desde el último `dev` que nació bien (`ab18d1a`) sólo afecta a `pre_destroy` (camino de *destroy*), a `--service ""` y a una línea de documentación en los tipos. Nada del camino de `launch` |
| 09-11 12:08 | ídem | Se lanzó un `dev` desde la laptop **con el código de ayer sin tocar** | Nació bien: 272 s del arranque a `DEV_READY`. El fallo **ya no era reproducible antes de cambiar nada** |
| 09-11 12:18 | ídem | Se repitió **desde el mini**, que es la máquina que había fallado | También nació bien, 348 s. Dos intentos, dos éxitos, con el código exacto de la noche del fallo |
| 09-11 ~12:20 | ídem | **Hallazgo de fondo** — el defecto que hizo que anoche no se pudiera saber nada | Una sonda SSH que falla deja `stdout` vacío, **idéntico a un «todavía no»**. O sea que «la máquina va lenta» y «no llego a la máquina» —cuyos arreglos no se parecen en nada— caían en la misma rama muda durante quince minutos. Y el `stderr`, que sí lo distinguía, se tiraba |
| 09-11 ~12:25 | ídem | Se instrumentó la espera (`40bf2dd`) | La espera mira `stderr` y el código de salida; el diagnóstico va **dentro** del `die()` porque desde el móvil el coordinador sólo publica `stderr`; el error avisa de que el droplet sigue vivo y facturando; plazo 900 → 1800 s (`DO_DEV_TOOLS_TIMEOUT`) |
| 09-11 ~12:28 | ídem | **Hallazgo:** la duración del arranque es una lotería, y la echa `apt-daily` | Su timer lleva un retardo **aleatorio de hasta 12 h**: medido en dos droplets recién creados, el siguiente disparo salía a las 23:36 en uno y a la 01:46 del día siguiente en el otro. Si cae durante el arranque, compite por el cerrojo de dpkg y cloud-init espera lo que haga falta. Mitigado parándolo en `bootcmd` y devolviéndolo al final de `runcmd`: se desactiva la carrera, no las actualizaciones |
| 09-11 ~12:32 | ídem | **Objeción del usuario:** «pero si ya cambiaste el código, eso puede haber corregido el problema, y no lo verías nunca» | Correcta en la mitad que importa: subir el plazo convertía el arranque patológico en un **éxito mudo**, cuando antes al menos fallaba y se notaba. Arreglado en `7c6e481`: por encima de 600 s la espera denuncia cuánto tardó **aunque acabe bien**, por `stdout`, que es lo que el coordinador publica cuando el código es 0 |
| 09-11 12:41 | ídem | El usuario destruyó el `dev` y lo relanzó desde Telegram para observarlo | Nació bien: 293 s, timers `inactive` durante el arranque y `active` al terminar, los tres servicios puestos, nodo Tailscale llamándose `dev` |
| 09-11 ~12:45 | ídem | **Petición:** «analiza qué hace que le lleve tanto tiempo el arranque. Sólo queremos que deje el dev funcional al nivel básico, los otros detalles los puede cargar según la demanda» | — |
| 09-11 ~12:50 | ídem | Desglose medido en un `dev` real (293 s de la API a `DEV_READY`) | **119 s el `dist-upgrade` de 148 paquetes**; 57 s el resto del módulo de apt; 45 s los scripts de DigitalOcean; **40 s el `runcmd` entero, con Node, Claude Code, gh y uv dentro**; 16 s los `packages:` de la plantilla; 26 s de la API al kernel. O sea que **lo que justifica la máquina costaba 40 s**, y el 40 % del arranque se iba poniendo al día un sistema que va a vivir horas |
| 09-11 ~12:50 | ídem | **Hallazgo que corrige una intuición:** `packages:` no vale la pena tocarlo | El instinto decía «quita `build-essential`». La medición dice **16 s de 293**. No hay nada que ganar ahí. Queda escrito para que nadie lo recorte a ojo |
| 09-11 13:09 | ídem | `package_upgrade: false` en las dos plantillas (`bf960dc`), verificado en droplet real | **168 s contra 293 s**; el módulo de paquetes de 192 s a 68,8 s, con todas las herramientas puestas igual. `unattended-upgrades` viene armado y `enabled`, así que los parches entran después en segundo plano. Y de los 148 paquetes, **6 eran de `openssh` o del kernel**: sacarlos del primer arranque quita además el riesgo, ya conocido en ese repo, de que sshd se quede sin arrancar |
| 09-11 ~13:15 | ídem | **Dato del usuario:** anoche, tras el fallo, le escribió al bot de aquel `dev` y no contestó nunca — «no fue sólo un retraso, algo más se había dañado» | **No se había dañado nada: estaba a medio hacer.** `wait_for_dev_tools` está *antes* de `provision`, así que al morir la espera no se empujaron secretos, no se clonaron repos y **no se instaló ningún servicio**. El bot no contestaba porque no estaba instalado. El error no lo decía; ahora sí |
| 09-11 ~13:25 | `digital-ocean-dropplet-auto-launching` + `claude-code-webapp-mobile` | **Corrección de una creencia registrada como hecho.** `CLAUDE.md` del lanzador afirmaba, como pendiente del usuario, que la `CWEB_TS_AUTHKEY` «no es Ephemeral». El usuario sostuvo que siempre lo ha sido | **Tenía razón, y el origen era una cita mal hecha.** `nodo.mjs` nunca afirma eso: emite un aviso de **deriva de nombre** («este nodo no se llama `dev`, se llama `dev-1`») y al final una nota **condicional** que dice explícitamente *no toques la authkey* salvo que un nodo recién caído siga registrado al día siguiente. Ese condicional se resumió como afirmación plana, y al vivir en `CLAUDE.md` se recargaba en cada sesión y se repetía. Comprobado hoy: la tailnet tiene sólo `dev` (online) y el móvil, sin nodos muertos acumulados. La causa real de los `dev-1`/`dev-2` de ayer ya estaba identificada: quien destruyó corría código anterior a `pre_destroy`, así que el `tailscale logout` nunca llegó a ejecutarse |
| 09-11 ~13:30 | `estudios-redes-neuronales` | **Petición:** crear en el repo central una bitácora donde se anote todo lo que se pide y todos los hallazgos según ocurren, de todos los repos, donde **nunca se modifican filas anteriores, sólo se añaden nuevas** | Creada: [`bitacora/`](../README.md), con un fichero por mes. La regla de sólo-añadir se justifica sola con lo de hoy: reconstruir la tarde anterior costó una mañana porque sólo quedaban los commits —que cuentan el resultado, no el camino— y un log de conversación que nadie recordaba dónde estaba. Lo que hacía falta era **la creencia equivocada del momento** |
| 09-11 ~13:30 | todos | **Convención que sale del caso de la authkey**, anotada porque vale para cualquier repo | Un aviso **condicional** no se resume como afirmación. «Sólo si X, entonces Y» escrito como «Y» se convierte en un hecho falso que además se reinyecta en cada sesión si vive en un `CLAUDE.md`. Al copiar una advertencia de un programa a una nota, se copia **con su condición** o no se copia |
| 09-11 ~13:40 | `digital-ocean-dropplet-auto-launching` | Al propagar los cambios a la flota, la API de DigitalOcean empezó a devolver **401** | **El `DO_TOKEN` dejó de valer entre las 13:09 y las 13:40 UTC**: a las 13:09 aún destruía droplets y a las 13:40 `/v2/account` contesta 401. El `.env` no se ha tocado desde el 09-10 15:18, así que el token no cambió aquí: lo invalidó DigitalOcean (revocado o caducado). Afecta a **toda la flota a la vez**, porque las tres máquinas llevan copia del mismo: comprobado, 401 también desde el mini y desde el dev, o sea que el bot de Telegram tampoco puede crear ni listar droplets. Lo que **sigue funcionando** es todo lo que no pasa por la API: SSH (las claves son independientes), `git pull`, los servicios y el `update`, que se hizo por SSH directo sin problema. ⚠ Y una comprobación que parecía un control y no lo era: `/v2/sizes` sin token también da 401, porque DigitalOcean **no tiene endpoints públicos** — a diferencia del catálogo de Vast.ai, que sí responde sin autenticar y por eso allí sí sirve para distinguir «token malo» de «red caída» |
| 09-11 ~13:50 | `digital-ocean-dropplet-auto-launching` | Se repartió el token nuevo con `push-do-token` y se comprobó sitio por sitio | **Un token rotado tiene más destinos de los que parece, y la foto del proceso es uno de ellos.** Tras escribir el nuevo en `dev-secrets.env` de las dos máquinas: la sesión SSH del mini ya autenticaba, pero el **proceso del bot** seguía con el viejo (401) en las DOS máquinas — el entorno de un proceso es una foto de cuando arrancó, así que escribir el fichero no basta: hay que reiniciar el servicio. Y en el mini había además una segunda copia viva: `~/src/telegram-coordinator/.env` con su propio `DO_TOKEN`, también muerto. El dev no tenía esa copia y heredaba de `dev-secrets.env` |
| 09-11 ~13:55 | ídem | **⏳ HILO QUE SE QUEDA ABIERTO:** reiniciar el bot del dev **no** arregló su token | Con `dev-secrets.env` ya actualizado y sin `DO_TOKEN` en el `.env` del servicio, tras `systemctl restart telegram-coordinator` el proceso nuevo **seguía dando 401**. No se llegó a averiguar por qué —faltó mirar si `dev-secrets.env` tenía dos líneas `DO_TOKEN` con la vieja ganando, que es la sospecha— porque la máquina se destruyó antes. Queda anotado sin resolver: si `push-do-token` deja una copia vieja por detrás, volverá a morder en la próxima rotación |
| 09-11 ~13:58 | ídem | **Hallazgo al preparar el mini nuevo:** `TGL_DO_TOKEN` del `.env` de la laptop era el token **revocado** | Comparado contra el `DO_TOKEN` viejo que aún tenía el bot del mini: **el mismo valor**. O sea que no era un token distinto a propósito, era una copia que se quedó atrás en una rotación anterior. Sin arreglarlo, el mini recién creado habría nacido con un Lanzador incapaz de crear ni destruir nada, y el síntoma —un bot que contesta pero falla al gastar— no se parece a «el token está viejo». Puesto al día antes de lanzar: HTTP 200 |
| 09-11 ~14:00 | ídem | **Petición:** «destruye todo, y vuelves a crear sólo el mini». Acto seguido: «mejor yo los destruyo» / «destruidos» | Ejecutado por el usuario; cuenta vacía comprobada con `list`. El `mini` se recrea desde la laptop. ⚠ Nota de operación: con el token del bot muerto, **destruir desde Telegram no habría funcionado** — la única vía era la laptop |
| 09-11 14:05 | `digital-ocean-dropplet-auto-launching` | `mini` recreado desde la laptop, y verificado | Nació en **156 s** (13:47:40 → `DEV_READY` 13:50:16), con el módulo de paquetes en 48 s y los timers de `apt-daily` devueltos — primera medición de la plantilla `mini` con `package_upgrade: false`. IP nueva `142.93.202.63`: lo que apuntara a la anterior deja de resolver. Bot `telegram-launcher` activo y, lo que importaba, **su proceso autentica contra la API (HTTP 200)** — nació con el token bueno porque `TGL_DO_TOKEN` se arregló ANTES de lanzar. Paridad correcta: llavero 13/17 (las 4 que faltan son opcionales) y clave de flota puesta. Y una lectura del hilo abierto de las 13:55: aquí hay **una sola** línea `DO_TOKEN` en `dev-secrets.env` y una en el `.env` del bot, así que el fallo del dev no se reproduce en una máquina recién hecha |
| 09-11 14:33 | `digital-ocean-dropplet-auto-launching` | **El usuario informa de que el fallo SIGUE:** lanzó un `dev` desde Telegram, esperó 30 min y nada; y al escribirle al dev, tampoco contesta | Encontrado en vivo, con el `launch` todavía corriendo. **El droplet estaba LISTO desde hacía 21 minutos** (`DEV_READY` a las 14:12:22, arranque de 148 s) y el lanzador no lo veía: su proceso llevaba 1467 s **durmiendo**, sin hijos y sin conexiones. O sea que NO era lentitud —lo que esta mañana se dio por «la causa más probable»— sino la otra rama, la que no se pudo distinguir el día anterior |
| 09-11 14:36 | ídem | **LA CAUSA DE VERDAD, y es de otra familia:** el entorno de un servicio es una **FOTO de cuando arrancó** | `provision` instala y **arranca** los servicios, y `hacer_lanzador()` —que escribe `DO_FLEET_KEY_FILE` y `DO_SSH_KEY_FILE` en `dev-secrets.env`— corre **después**. El bot del mini nunca ve esas variables (cero coincidencias en `/proc/<pid>/environ`), así que todo `launch` que sale de él cae al defecto `~/.ssh/do_droplet`. Y en un mini recién hecho esa clave local **no está registrada en la cuenta** (huella `d7:75:2a…` contra la `bb:ae:e4…` registrada), así que **ningún droplet nuevo la aceptará jamás**. Probado contra el droplet del usuario: con `do_droplet`, `Permission denied`; con `do_flota`, `READY` |
| 09-11 14:36 | ídem | **Por qué no se reprodujo esta mañana**, que es la lección que más duele | La prueba «desde el mini» se hizo con `ssh … bash -lc`, y **un shell de login SÍ sourcea `dev-secrets.env`**. O sea que se probó exactamente el único entorno que no falla. El mismo comando, dos entornos, y sólo uno roto. Para ejercitar el camino del bot hay que usar el buzón `data/entrada/`, no SSH — y eso ya estaba escrito en el `CLAUDE.md` del lanzador desde el 09-10, sin usarse |
| 09-11 ~14:40 | ídem | **Corrección de algo dicho hace minutos:** se informó de que reiniciar el bot no arreglaba el token | Era falso: se había mirado un **pid equivocado** (elegido con `pgrep` justo durante el reinicio). El `MainPID` real del servicio sí tenía la variable. Se corrige aquí en vez de borrar nada, que es la regla de esta bitácora |
| 09-11 14:50 | ídem | Arreglado y **verificado por el camino que fallaba** (`e1b77b3`) | Dos arreglos: `provision` llama a `reiniciar_servicios()` al final, después de `hacer_lanzador` —lo que cierra la **clase entera**, no este síntoma: cualquier variable escrita tarde nacía invisible—; y una clave rechazada mata la espera en ~1 min diciendo **con qué clave** se intentaba, en vez de sondear media hora, porque un droplet sólo acepta las claves registradas cuando se creó. Verificación: `launch dev` inyectado por el buzón del bot → droplet completo con sus tres servicios, paridad correcta en las dos máquinas, 6/6 ficheros de test en verde |
| 09-11 ~14:55 | ídem | **Hallazgo secundario, sin arreglar:** `provision` no aplica el tipo | Reparar un droplet a medias con `provision dev` **no** clona los repos del tipo, ni instala sus servicios, ni corre sus `post` —no existe `--type` en ese subcomando—. La máquina queda funcional pero coja, y la única salida es rehacerla. Se descubrió al intentar rematar el dev que había quedado a medias |
| 09-11 ~14:55 | ídem | **Y un fallo en un test escrito hoy**, destapado por otro test | Parchear `limpio.time.time` toca el módulo `time` **global** —`importlib` da un módulo nuevo para `do_droplet`, no para lo que éste importa—, así que el reloj falso se quedaba puesto y los casos siguientes no vencían nunca: colgaban la suite. Restaurado y anotado en el propio test |
| 09-11 ~19:30 | `digital-ocean-dropplet-auto-launching` | **El usuario informa de que el fallo VOLVIÓ, ahora al contrario:** «tenemos problemas para lanzar el mini desde el dev», con el error pegado — `root@161.35.50.148: Permission denied (publickey)`, intentando `/home/deploy/.ssh/do_droplet` | El mensaje nuevo de `e1b77b3` hizo su trabajo: dijo la clave, dijo que un droplet sólo acepta las registradas al crearse, y avisó de que la máquina seguía facturando. O sea que el diagnóstico costó minutos en vez de una mañana. Lo que **no** estaba arreglado era el fallo |
| 09-11 ~19:35 | ídem | Se comprobó el estado real del droplet antes de tocar nada | **El droplet estaba PERFECTO.** Entrando desde la laptop con `~/.ssh/do_flota`: `cloud-init status: done` y `DEV_READY` puesto. Y a medio hacer, como dice el error: `/home/deploy/src` vacío y `telegram-launcher` inactive, porque `launch` muere **antes** de `provision`. No había nada que destruir: lo que no valía era la clave elegida en el lado que lanza. `list` confirmaba `mini` (161.35.50.148) y `dev` vivos, 28,00 $/mes |
| 09-11 ~19:40 | ídem | **Hallazgo que corrige el alcance de `e1b77b3`, no su contenido:** `reiniciar_servicios()` arregla las máquinas que **nazcan** desde entonces, y no las que ya estaban vivas | El bot del dev se había puesto en marcha antes de que existiera su `DO_SSH_KEY_FILE`, así que su foto de entorno no la tiene y `cfg()` cae al defecto `~/.ssh/do_droplet`. Comprobado en el dev: `dev-secrets.env` **sí** dice `DO_SSH_KEY_FILE=/home/deploy/.ssh/do_flota`, o sea que por SSH el mismo comando funciona. Es la trampa del 09-11 14:36 por la otra punta: entonces era el mini lanzando un dev; ahora un dev lanzando el mini |
| 09-11 ~19:45 | ídem | **Y el detalle que tira por tierra el arreglo evidente**, hallado al mirar el disco del dev en vez de suponerlo | La caída natural era «si `DO_SSH_KEY_FILE` no existe, usa la de la flota». **No habría servido:** `/home/deploy/.ssh/do_droplet` **existía** en el dev —creado hoy a las 18:22 con `keygen`, comentario `dev`— y su pública **no estaba en la cuenta** (verificado contra `/v2/account/keys`: 5 claves, ninguna con ese material). O sea que **un fichero de clave que existe y no autentica es tan inservible como uno que falta**, y mirando el disco no se distinguen: se distinguen preguntándole a la cuenta |
| 09-11 19:50 | ídem | Arreglado en dos capas (`7942e2d`, `e857cc7`), con 13 tests nuevos | `fichero_clave_ssh()` cae a la clave de la flota cuando la configurada no existe —local e instantánea, que es lo que tiene que ser: por ahí pasa todo el `ssh` del lanzador—. Y `launch` comprueba **antes de crear** que la clave esté entre las que el droplet llevará (`comprobar_clave_de_entrada()`, antes incluso de preguntarle a GitHub); si no lo está y la de la flota **sí está registrada**, cambia a ella en vez de morir, porque no se muere teniendo delante una clave que sí entra. Que la de flota valga no es adivinar: está registrada por definición y `DO_SSH_KEYS` vacío mete todas las de la cuenta en cada droplet nuevo. Verificado: los 7 ficheros de test en verde en PowerShell, y `comprobar_clave_de_entrada()` ejercitada contra la cuenta real sin crear nada |
| 09-11 19:50 | ídem | **Lección de método, que es la que vale para otros repos** | El arreglo de la primera capa era el que dicta la intuición y **habría dejado el fallo intacto**, con dos commits y una lección escrita encima. Lo que lo destapó fue ir a mirar el estado real de las dos máquinas —qué ficheros hay, con qué fecha, y qué dice la cuenta de ellos— en vez de razonar sobre el código. La forma corta: cuando el síntoma es «credencial rechazada», **existir y servir son dos preguntas distintas**, y sólo la segunda se le puede hacer a quien la valida |
| 09-11 ~20:05 | `digital-ocean-dropplet-auto-launching` | **Corrección de una fila de hace minutos (~19:45), que es para lo que existe esta bitácora.** Se anotó que el `~/.ssh/do_droplet` del dev lo había creado `keygen` a las 18:22. **Falso, y la verdad es mucho peor** | **Lo crea el `post` del propio tipo.** `VAST_SSH_KEY_FILE` en `vast_instance.py` tiene por defecto **la misma ruta** que `DO_SSH_KEY_FILE` —`~/.ssh/do_droplet`—, y los `post` de `types/dev.json` y `types/mini.json` corren `vast_instance.py register-key`, que genera ese par y lo registra **en Vast.ai y no en DigitalOcean**. Visto en vivo al recrear el mini: «No hay clave en `/home/deploy/.ssh/do_droplet`; generando un par ed25519 … Clave registrada en Vast.ai», comentario `mini`. O sea que **toda máquina de la flota tiene ahí un fichero que existe y no sirve para entrar en un droplet**: el caso de hoy no era la casualidad de un dev, era **el estado normal**. Dos proveedores compartiendo un literal por defecto; los scripts no comparten código a propósito, pero sí heredaron esa ruta |
| 09-11 ~20:05 | ídem | **⏳ PENDIENTE DEL USUARIO que sale de ahí:** cambiar `VAST_SSH_KEY_FILE` a algo como `~/.ssh/vast` | Se autoarregla —`asegurar_clave_local()` registra la nueva—, pero **las instancias de Vast alquiladas antes no la aceptarán**, porque su lista de claves se fija al crearlas. Por eso no se toca sin que el usuario lo decida |
| 09-11 20:00 | ídem | `mini` rehecho desde la laptop y paridad comprobada | El de 161.35.50.148 se destruyó —estaba vivo, facturando y sin bot— y nació uno nuevo en `67.205.139.15`. `flota` da paridad correcta en las dos máquinas: llavero 13/17 (las 4 que faltan son opcionales), clave de flota puesta, `telegram-launcher` en el mini y los tres servicios en el dev. ⚠ La IP cambió otra vez: lo que apuntara a `161.35.50.148` deja de resolver |
| 09-11 ~20:00 | ídem | **Los dos arreglos de hoy resultaron complementarios, hechos en paralelo sin saberlo el uno del otro** | El del dev (`8bd3934`) hace que los **once** ejecutores carguen `dev-secrets.env`, así que la foto del entorno deja de importar y vale en el mensaje siguiente sin reiniciar nada. El de la laptop (`67b8e59`, `3d899ef`) hace que **la elección de la clave no dependa del entorno** y añade la comprobación gratuita antes de crear. Se integraron con un rebase, sin conflictos y con los 7 ficheros de test en verde. Ninguno hace redundante al otro: el primero arregla el entorno, el segundo aguanta que el entorno esté mal |
| 09-11 20:05 | ídem | Verificado **por el camino que fallaba**, en el dev real | Con `env -u DO_SSH_KEY_FILE` —el entorno pelado del bot—: elige `/home/deploy/.ssh/do_droplet`, detecta que **no está registrada en la cuenta**, avisa y cambia a `/home/deploy/.ssh/do_flota`, que sí lo está. Antes, eso eran seis sondas rechazadas y un droplet vivo a medio hacer |
| 09-11 ~20:20 | `digital-ocean-dropplet-auto-launching` | **Petición: «arregla vast».** `VAST_SSH_KEY_FILE` pasa de `~/.ssh/do_droplet` a `~/.ssh/vast` (`43ade8b`) | Seguro de hacer ahora y comprobado antes de tocar: **cero instancias vivas** en Vast, así que ninguna máquina alquilada se queda sin la clave que aceptaba —la lista se fija al crearlas—. Y al abrir el `.env.example` salió que **no era un literal heredado, era una decisión escrita**: «por defecto la misma que la de DigitalOcean». **Cuando se escribió era razonable**, porque los droplets se entraban con esa clave; lo que la volvió trampa fue la **clave de flota**, que movió DigitalOcean a `~/.ssh/do_flota` y dejó la ruta vieja ocupada por un fichero inservible. La forma de la lección, que vale para cualquier repo: **una decisión correcta se convierte en trampa cuando cambia lo que la hacía correcta, y nada avisa** — el comentario que la justificaba sigue ahí, leyéndose como si aún valiera |
| 09-11 ~20:25 | ídem | Migradas las dos máquinas vivas, y **no con el mismo gesto que la laptop** | En una máquina de la flota `~/.ssh/do_droplet` es **sólo** la clave de Vast (la de DigitalOcean es `do_flota`), así que ahí se **renombra** y la clave sigue registrada en Vast sin tocar la cuenta. **En la laptop no se puede**: ahí `do_droplet` *es* la clave de DigitalOcean, y moverla dejaría la máquina sin acceso. Comprobado antes de renombrar que en las dos `DO_SSH_KEY_FILE` apuntaba a `do_flota` y que `VAST_SSH_KEY_FILE` no estaba pisado en ningún `.env`; después, que `vast_instance.py keys` sigue marcando la clave de cada una. Y el efecto secundario bueno: al desaparecer el fichero trampa, el camino de DigitalOcean ya elige `do_flota` **de primeras**, sin necesitar la comprobación contra la cuenta |
| 09-11 ~20:35 | ídem | **Y separar las rutas destapó un hueco que la coincidencia tapaba** (`f0a39ca`) | **`launch` de Vast no comprobaba la clave**: alquilaba y después decía «entra con `-i ~/.ssh/vast`», un fichero que en una máquina ya existente no está. No se notaba porque el `post` del tipo corre `register-key` al crear cada máquina de la flota, así que la clave ya estaba hecha y registrada y `launch` no tenía que comprobar nada. Al mover la ruta ese apoyo desaparece y el primer `launch` habría alquilado una instancia **inaccesible y facturando**. Arreglado con `asegurar_clave_registrada()` **dentro de `alquilar()`** —no en `cmd_launch`— porque hay **dos** sitios que gastan, `launch` y el barrido, y un guard que hay que recordar en cada sitio nuevo es una nota y no un arreglo. Verificado ejecutándolo: generó la clave de la laptop, la registró (id 1356102, sale marcada en `keys`) y la segunda llamada es muda |
| 09-11 ~20:35 | ídem | **Lo que esto dice de arreglar un acoplamiento**, anotado porque es lo reutilizable | Quitar una coincidencia entre dos cosas **no es sólo cambiar el valor**: lo que se apoyaba en ella deja de funcionar, y no siempre en el sitio que se está tocando. Aquí el apoyo estaba a dos repos de distancia del cambio —en el `post` de un tipo de DigitalOcean— y el fallo habría salido en el primer alquiler de Vast, que es donde nadie lo estaría buscando. Buscar **quién se apoyaba** en la coincidencia es parte del arreglo, no una revisión posterior |
| 09-11 ~20:55 | `digital-ocean-dropplet-auto-launching` | **Petición: «deja todo limpio».** Se interpretó como la limpieza ofrecida —claves muertas acumuladas— y **no** como un barrido de máquinas | ⚠ Dicho explícitamente antes de tocar nada: «limpia» **no** significó destruir droplets. El `mini` no se toca nunca en una limpieza y el `dev` estaba trabajando; siguen los dos vivos, 28,00 $/mes. Primero el inventario: se leyó el material de clave de las tres máquinas vivas (laptop, mini, dev) y se cruzó contra las dos cuentas, en vez de deducir por el nombre |
| 09-11 ~21:00 | ídem | Vast: **44 claves, 3 de máquinas vivas**. Podadas 40 (`e3772c0`) | Una por cada máquina que existió alguna vez, el mismo goteo que las 26 `lanzador-dev` de DigitalOcean. Pero **no se poda igual, y no es cuestión de gusto**: en Vast las claves **no tienen nombre** —la API devuelve id y material—, así que el patrón por nombre no existe y `keys --prune` borra «todas menos las protegidas»: siempre la de esta máquina, más las de `--keep <id>`. Las de otras máquinas vivas **hay que declararlas**, porque desde una máquina no se adivinan las de las demás y borrarlas la deja sin poder entrar en lo que alquile. Quedaron 3 (dev, mini, laptop) y se comprobó **después, en las dos máquinas de la flota**, que `keys` sigue marcando la suya |
| 09-11 ~21:00 | ídem | **Y por qué en Vast se puede barrer y en DigitalOcean no** | Porque `asegurar_clave_registrada()` hace que cualquier máquina que alquile **registre su clave sola antes de gastar**: una borrada de más se repone en el siguiente `launch`. En DigitalOcean no hay red equivalente —un droplet fija sus claves al crearse—, así que allí una clave borrada por error no se arregla sola y hay que volver a registrarla a mano. **El mismo barrido es seguro o irreversible según lo que haya debajo**, y eso hay que mirarlo antes de borrar, no después |
| 09-11 ~20:58 | ídem | El endpoint de borrado **no estaba en la spec**, y se probó con UNA clave antes de barrer | `DELETE /api/v0/ssh/{id}/` → `{"success": true}`, y la cuenta baja de 44 a 43. Es la regla de esta API otra vez —validar contra la respuesta real y no contra la documentación—, con el añadido de que **si la operación es destructiva, se prueba con un solo elemento primero** |
| 09-11 ~21:05 | ídem | **⏳ QUEDA PARA EL USUARIO: tres claves de DigitalOcean que parecen muertas y una puede no serlo** | `mini` (58461185, comentario `do-droplet`) y `foveal` (58519827, `foveal-droplet-trabajo`) no están en ninguna máquina viva. La tercera, `AIG0009690-do-droplet` (58449868, `do-droplet-20260811`), **parece otra laptop del usuario** —hay constancia de que trabaja también desde una máquina de oficina—, y borrarla la dejaría fuera de todos los droplets NUEVOS sin que nada avise. Aquí el barrido **no** se autocorrige, al contrario que en Vast, así que no se toca ninguna sin que el usuario diga cuál |
| 09-11 ~21:10 | `digital-ocean-dropplet-auto-launching` | **Decisión del usuario sobre las claves de DigitalOcean:** borrar sólo `mini` y `foveal`; dejar `AIG0009690-do-droplet` | Hecho con `keys --prune` por nombre: la cuenta pasa de 5 a 3 claves —`flota`, `DESKTOP-RLVMP0N` (esta laptop) y la de la otra laptop, que se conserva a propósito—. Cierre de la limpieza: **Vast 44 → 3, DigitalOcean 5 → 3**, cero instancias en Vast, y en DigitalOcean siguen vivos `mini` (67.205.139.15) y `dev` (159.223.136.203) con paridad correcta. Los dos repos limpios y empujados, la flota al día con `e3772c0` |
| 09-12 ~02:30 | `digital-ocean-dropplet-auto-launching` / `claude-code-webapp-mobile` | **Petición:** «hubo una falla de tailscale, que no me permite un nuevo certificado para que se conecte el app al dev. Analiza si tenemos opciones que me permitan lo mismo. Ya probé con usar http, pero eso supuestamente me impide instalar en el home del cell» | — |
| 09-12 ~02:40 | ídem | Se buscó de quién es el fallo | **No es de Tailscale: es el límite de Let's Encrypt** de 5 certificados por semana para el mismo nombre exacto, y cada `dev` rehecho pide uno nuevo para `dev.tail376e31.ts.net` porque el certificado vive en `/var/lib/tailscale/certs/` del droplet que se destruye. Comprobado en el `dev` vivo: el almacén sólo tiene `acme-account.key.pem`, y el journal de `tailscaled` dice `429 rateLimited … retry after 2026-09-12 20:03:36 UTC`. Y lo del `http` es cierto: sin contexto seguro no hay service worker y Android no ofrece «Añadir a pantalla de inicio» (la propia app lo documenta en su README) |
| 09-12 ~02:50 | ídem | **Hallazgo, leído en el código de Tailscale** (`feature/acme/certstore.go`): un certificado ya presente en el almacén **se reutiliza sin pedir otro** | `certFileStore.Read` lee `<dominio>.crt`/`.key`, los valida contra las raíces del sistema y las de Let's Encrypt embebidas, y si valen y no han caducado los sirve; sólo renueva en segundo plano a los 2/3 de la vida (~día 60 de 90), y mientras tanto sigue sirviendo el viejo. O sea que **el certificado puede viajar con la flota** como ya viaja la clave SSH de flota, y rehacer el `dev` costaría 0 certificados en vez de 1. Un autofirmado en ese almacén, en cambio, se rechaza salvo que su CA esté en las raíces del sistema del droplet |
| 09-12 ~02:55 | ídem | **Hallazgo:** `tailscale serve` acepta destinos remotos (`isRemote` en `cmd/tailscale/cli/serve_v2.go`, sólo avisa en macOS) | Abre la alternativa de que **el mini sea la puerta fija**: nunca se rehace, así que su certificado se pide una vez y se renueva solo; haría de proxy hacia `http://dev:8080` por la tailnet, y la URL de la PWA dejaría de depender de que el nodo `dev` recupere su nombre. Cuesta instalar `tailscaled` en 512 MB (55 MB libres medidos, con 1 GB de swap) y cambiar la URL una vez |
| 09-12 ~03:00 | ídem | Veredicto entregado al usuario, **sin tocar nada**: recomendada la persistencia del certificado en el llavero; el mini como puerta fija es la alternativa estructural; una CA propia instalada en el móvil queda como último recurso. Lo inmediato: a partir del 09-12 20:03 UTC volver a `https` en el `dev` vivo, y **guardar** ese certificado antes de rehacer el `dev` otra vez | — |
| 09-11 23:14 | ídem | **Corrección de las cinco filas anteriores:** las horas «09-12 ~02:30 … ~03:00» están mal | Se escribieron a ojo antes de mirar el reloj. Lo real, medido con `date -u`: todo ocurrió el **09-11 entre las ~22:40 y las 23:12 UTC**. No se tocan las filas; se dice aquí |
| 09-11 ~23:20 | `digital-ocean-dropplet-auto-launching` / `claude-code-webapp-mobile` | **Corrección del usuario a la fila de las 23:12:** «el mini sí se rehace, cuando le tengo que hacer cambios, y se rehace desde el dev. Ahora tanto el mini como el dev son efímeros, porque el uno crea y destruye al otro, y eso me permite actualizar el código del otro». Y elige la opción 1: el certificado viaja con la flota | «Siempre encendida» quiere decir que siempre hay **una** encendida, no que sea la misma. Se lleva a `CLAUDE.md`, al README y a `types/mini.json`. Consecuencia para cualquier cosa nueva: nada que esté sólo en el mini sobrevive |
| 09-11 23:56 | ídem | **Implementado**, en dos repos: `34b38d7` (app) y `763e85e` (lanzador). El certificado se pide una vez, va al llavero como `CWEB_TS_CERT_B64`/`CWEB_TS_KEY_B64`, y cada dev nuevo lo coloca en `/var/lib/tailscale/certs/` antes del `serve`, sólo si vale para el nombre que la tailnet dio. El esquema por defecto lo decide el dato: `https` si hay certificado, `http` si no; nunca se pide uno sin escribir `CWEB_TS_ESQUEMA=https` | Y dos mecanismos genéricos que faltaban: **`entornos recoger`**, el simétrico de `aplicar` (lo que un proyecto produce vuelve al llavero, sólo lo declarado, pisando porque el productor es la fuente), y **`load_env()` lee `dev-secrets.env` del disco**: antes una variable enviada al llavero después de arrancar el bot era invisible para todo `launch` desde el bot hasta reiniciarlo. Con el certificado eso habría sido un dev naciendo sin él y pidiendo otro sin decirlo. 22 tests en la app, 6 en el lanzador; desplegado en `dev` y `mini` con `remoto … update`, 155/155 en el dev |
| 09-11 23:56 | ídem | **Lo que NO está visto:** ningún certificado real, porque Let's Encrypt no reabre hasta el **2026-09-12 20:03 UTC**. Queda como pendiente del usuario en `CLAUDE.md`, con los cuatro pasos y lo esperado en cada uno en `claude-code-webapp-mobile/docs/pendiente-verificar.md` | En el dev vivo, comprobado hoy: `cweb cert` dice «en tailscaled: NO hay ninguno; en el llavero: NO», el `--seco` de unir dice «certificado: NO viene», y `entornos recoger` falla honestamente con «no hay certificado en tailscaled … nada que exportar» y exit 1 |
